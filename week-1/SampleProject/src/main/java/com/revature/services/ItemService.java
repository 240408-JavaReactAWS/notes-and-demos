package com.revature.services;

public class ItemService {

    /*
    Service classes are used for business logic and are the bridge between the controller (WEB) layer and the repo (DATA)
    layer. These guys will take a call from the controller, get the appropriate data from the db (or execute the proper
    query) and then return the result back to the controller
     */
}
