package com.revature.controllers;

public class ItemController {

    /*
    This controller uses Spring Web MVC to handle HTTP requests sent to it. If you send a GET, POST, PUT, DELETE, etc.
    request to the application at the proper endpoint, this class will execute its logic and call upon the service layer
    as needed to execute queries against the database (reading, writing, updating, deleting)
     */
}
