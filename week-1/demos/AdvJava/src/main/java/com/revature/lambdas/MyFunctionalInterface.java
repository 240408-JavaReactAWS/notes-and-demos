package com.revature.lambdas;

public interface MyFunctionalInterface {

    // Recall that all methods inside an interface are public and abstract by default
    public abstract int doMath(int a, int b);
    // Notice that public and abstract are gray because they are provided by default

}
